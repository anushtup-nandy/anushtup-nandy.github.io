# --- General ---
# Operating System Files
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# IDE/Editor Configurations
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
*.komodo*
*.*.sw[a-p] # Vim swap files
*.bak
*.tmp

# --- Node.js / Dependencies (Good practice even if not used yet) ---
# Dependency directories
node_modules/
# bower_components/ # Less common now, but sometimes seen

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov/

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# --- Build Output (Anticipating potential future build steps) ---
dist/
build/
public/ # Caution: Ensure 'public' doesn't contain source files you *want* tracked
out/
.parcel-cache/
.cache/
target/ # Common in some frameworks/build tools

# --- Environment Variables (CRITICAL to ignore) ---
# Ignore environment variable files
.env
.env.*
!.env.example # Typically, an example file IS committed

# --- Other ---
# Optional lock files (depends on package manager, but usually COMMIT these)
# package-lock.json # Usually committed
# yarn.lock         # Usually committed
# pnpm-lock.yaml    # Usually committed

# Mac specific system files created by archive utilities
*.cpgz
*.cpio

# Optional User-specific files or sensitive data
# secrets.json
# config.local.js